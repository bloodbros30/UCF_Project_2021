//makes sure the new chats are displayed correctly in options


import BrowseChats from './BrowseChats'

test('tests the display of chatOptions', () => {



  const chats = ["chat1", "chat2"];


    expect(chats.map()).toHaveBeenCalled();
    expect(useEffect()).toHaveBeenCalled();

  });
